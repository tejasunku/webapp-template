SPECIFICATION Spec

(* Check all invariants *)
INVARIANT Inv1 Inv2 Inv3 Inv4 Inv5

(* Check temporal properties - disabled for initial testing *)
(* PROPERTY NetworkHealing *)
(* PROPERTY ClientEventualResolution *)

(* Configuration for bounded model checking *)
CONSTANT
    MaxFactories = 3
    MaxClients = 4
    RecoveryTimeoutValue = 20
    ClientTypes = {Database, Cache, Queue, ExternalAPI}

(* Set reasonable bounds for state space exploration *)
(* SYMMETRY disabled for this model *)

(* Enable deadlock detection *)